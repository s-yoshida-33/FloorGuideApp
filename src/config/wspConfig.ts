// src/config/wspConfig.ts

export const WSP_CONFIG = {
    /**
     * Polling interval for WSP current asset in milliseconds.
     * This is intentionally set a bit higher to reduce CPU/network load.
     */
    pollIntervalMs: 2000,
  };
  